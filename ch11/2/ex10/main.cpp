#include <iostream>
#include <vector>
#include <list>
#include <map>

int main()
{
    std::map<std::vector<int>::iterator, int> vimap;
    std::map<std::list<int>::iterator, int> limap;
    return 0;
}
